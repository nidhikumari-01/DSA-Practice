// Basic Array Program: Sum of elements

let numbers = [10, 20, 30, 40, 50]; // array

let sum = 0;

for (let i = 0; i < numbers.length; i++) {
    sum = sum + numbers[i];       // adding each element
}

console.log("Sum of array elements is:", sum);

